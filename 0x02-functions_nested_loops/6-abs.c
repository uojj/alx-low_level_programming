#include "stdio.h"
/**
 * _abs - a function that prints the absolute value of an integer
 * @ab: integer input
 * Return: absolute value of ab
 */
int _abs(int);
{
  return (ab * ((ab > 0) - (ab < 0)));
}
