#include <stdio.h>

int main(void)
  
{
  
  // initializing strings
  
  char str1[] = "Programming is like building a multilingual puzzle";
  
  // writing to stdout
  
  puts(str1);
  
  return(0);
  
}
